name: Swift

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run tests
      run: xcodebuild clean test -project GitHubActionTest/GitHubActionTest.xcodeproj -scheme GitHubActionTest -destination 'platform=iOS Simulator,OS=16.0,name=iPhone 14 Pro'
